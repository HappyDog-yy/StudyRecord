### 暂时性死区TDZ

- 是否在暂时性死区之内，取决于**代码执行的顺序**，而非代码的写入顺序
- 函数1使用let声明的变量a，变量a的声明在该函数之后
- 但如果函数1的执行在变量a的声明之后，就不会出错reference error

- let、const、class声明的代码，在块开始执行代码
- 到声明和初始化变量的位置，称为暂时性死区
- 在暂时性死区中使用变量，会报错reference error

- 使用var声明的变量则不同
- 会进行变量提升，将声明提到作用域的最前面，而不会提升初始化的内容
- 因此在声明和初始化之前使用var声明的变量，不会出错，这些变量现在通通是undefinted

### 词法作用域

- 代码写在哪，变量的可用范围也会随之确定
- 内层函数天生能访问外层作用域的变量
- 这也是闭包的基础